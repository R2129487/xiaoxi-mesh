name: xiaoqing_app
description: 小青 App — 直连 Hermes MESH 的跨平台即时通讯客户端
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  web_socket_channel: ^3.0.1
  provider: ^6.1.2
  shared_preferences: ^2.3.3
  http: ^1.2.2
  flutter_local_notifications: ^18.0.1
  sqflite: ^2.3.0
  path: ^1.9.0
  path_provider: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
